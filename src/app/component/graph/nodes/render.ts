import { ClassicPreset as Classic } from 'rete';

export default class Render extends Classic.Node {

  constructor() {
    super('BRDF Shader');
    const socket = new Classic.Socket('socket');
    
    
  }

 
  
}
